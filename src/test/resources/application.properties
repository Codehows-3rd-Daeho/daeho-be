# Test application properties
spring.application.name=daeho-be-test
app.base-url=http://localhost:8080

# Allow bean definition overriding for tests
spring.main.allow-bean-definition-overriding=true

# H2 In-memory Database for testing
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate settings for testing
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace
spring.jpa.open-in-view=false

# Disable file upload limits for tests
file.location=/tmp
spring.servlet.multipart.max-file-size=-1
spring.servlet.multipart.max-request-size=-1

# Mocked or dummy values for external services
daglo.api.token=dummy-token
daglo.api.base-url=http://localhost:8089
daglo.api.timeout=5000

stt.recording.heartbeat-ttl-seconds=30

# Web Push VAPID Keys (dummy)
vapid.public.key=BPmR48nBJDQ3HLz9OOjjUq_dOCKmdERTcrCq8xwRQOGFVDD1lLtOeLq1ZFUM-JoSb2ZTICk84UCI16UWFNRn_rY
vapid.private.key=PK4blc0EL4gIBoxdpK1-5r7JfthqUa2_sa8MS9xgW04
vapid.service.mail=test@example.com

# Kafka (use dummy values)
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.admin.auto-create=true
logging.level.org.apache.kafka=WARN

# Redis (use dummy values, disable repositories)
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.repositories.enabled=false

# FFMPEG (dummy path)
ffmpeg.path=C:/ProgramData/chocolatey/lib/ffmpeg/tools/ffmpeg/bin/ffmpeg.exe

# JWT
jwt.key=dGVzdC1qd3Qta2V5LXRlc3Qtand0LWtleS10ZXN0LWp3dC1rZXkK
